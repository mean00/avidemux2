SET(ADM_LIB ADM_coreSqlLight3)
ADM_ADD_SHARED_LIBRARY(${ADM_LIB})
TARGET_SOURCES(${ADM_LIB} PRIVATE
  Database.cpp
  Query.cpp
  StderrLog.cpp
  SysLog.cpp)

IF(APPLE)
  IF(ADM_CPU_X86)
    TARGET_LINK_LIBRARIES(ADM_coreSqlLight3 PRIVATE -Wl)
  ENDIF()
ELSE()
  IF(UNIX)
    TARGET_LINK_LIBRARIES(ADM_coreSqlLight3 PRIVATE dl)
  ENDIF()
ENDIF()
TARGET_INCLUDE_DIRECTORIES(${ADM_LIB} PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/../include)
TARGET_LINK_LIBRARIES(${ADM_LIB}  PUBLIC ADM_core6 ${SQLITE3_LDFLAGS})
ADM_INSTALL_LIB(${ADM_LIB}) # API
#
