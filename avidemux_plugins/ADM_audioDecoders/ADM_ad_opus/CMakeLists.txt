INCLUDE(admCheckOpus)
checkOpusDec()

IF (LIBOPUS_FOUND)
	INCLUDE(ad_plugin)
	
	SET(ADM_ad_opus_SRCS ADM_opus.cpp)

	ADD_AUDIO_DECODER( ADM_ad_opus ${ADM_ad_opus_SRCS})
	TARGET_LINK_LIBRARIES(ADM_ad_opus ${LIBOPUS_LIBRARY_DIR})

	ADD_TARGET_CFLAGS(ADM_ad_opus "-I${LIBOPUS_INCLUDE_DIR}")

	INIT_AUDIO_PLUGIN(ADM_ad_opus)
	INSTALL_AUDIODECODER(ADM_ad_opus)
ENDIF (LIBOPUS_FOUND)
