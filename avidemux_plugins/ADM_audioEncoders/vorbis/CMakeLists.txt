INCLUDE(ae_plugin)

SET(ADM_ae_vorbis_SRCS audioencoder_vorbis.cpp)

ADD_AUDIO_ENCODER(ADM_ae_vorbis ${ADM_ae_vorbis_SRCS})

TARGET_LINK_LIBRARIES(ADM_ae_vorbis PRIVATE ${VORBIS_LIBRARY_DIR} ${VORBISENC_LIBRARY_DIR} )

TARGET_INCLUDE_DIRECTORIES(ADM_ae_vorbis PRIVATE "${VORBIS_INCLUDE_DIR}")

INIT_AUDIO_ENCODER(ADM_ae_vorbis)
INSTALL_AUDIOENCODER(ADM_ae_vorbis)
