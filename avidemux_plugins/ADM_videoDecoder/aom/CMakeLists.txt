INCLUDE(vd_plugin)
include(admCheckAom)
checkAomDec()
IF (USE_AOM)

SET(aom_SRCS
    aomDecPlugin.cpp
    ADM_aomDec.cpp
)
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})
INCLUDE_DIRECTORIES(${AOM_INCLUDEDIR})

ADD_LIBRARY(ADM_vd_aom SHARED ${aom_SRCS})
TARGET_LINK_LIBRARIES(ADM_vd_aom ${AOM_LDFLAGS})
INIT_VIDEO_DECODER(ADM_vd_aom)
INSTALL_VIDEO_DECODER(ADM_vd_aom)

ENDIF (USE_AOM)
