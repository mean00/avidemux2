INCLUDE(vd_plugin)
INCLUDE(admCheckVpx)
# LibVPX
checkVpxDec()
IF(USE_VPX_DEC)
  SET(vpx_SRCS
        vpxPlugin.cpp
        ADM_vpx.cpp
    )
  ADD_LIBRARY(ADM_vd_vpx SHARED ${vpx_SRCS})
  TARGET_INCLUDE_DIRECTORIES(ADM_vd_vpx PRIVATE ${CMAKE_CURRENT_BINARY_DIR})
  TARGET_INCLUDE_DIRECTORIES(ADM_vd_vpx PRIVATE ${VPX_INCLUDEDIR})
  TARGET_LINK_LIBRARIES(ADM_vd_vpx PRIVATE ${VPX_LDFLAGS})
  INIT_VIDEO_DECODER(ADM_vd_vpx)
  INSTALL_VIDEO_DECODER(ADM_vd_vpx)
ENDIF()
