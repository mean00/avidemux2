#
INCLUDE(Ts)
IF(NOT QT5_FOUND)
COMPILE_TS_FILES(${CMAKE_CURRENT_SOURCE_DIR} QM_FILES)
ADD_CUSTOM_TARGET(qmfiles ALL DEPENDS ${QM_FILES})
ENDIF(NOT QT5_FOUND)
MESSAGE(STATUS "i18n files : ${QM_FILES}")
